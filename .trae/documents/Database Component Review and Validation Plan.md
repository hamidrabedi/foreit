I will perform a comprehensive review and validation of the database-related components, focusing on code quality, testing setup, and verification.

### 1. Code Quality Assessment & Refactoring

* **Refactor** **`forge/orm/manager.go`**:

  * Extract hook execution logic into a shared helper method to remove duplication across `Create`, `Update`, `BulkCreate`, and `Delete`.

  * Centralize ID handling (reflection vs interface) to reduce code duplication and improve type safety.

  * Review `forge/orm` for other redundancy.

### 2. Database Testing Setup

* **Create Test Infrastructure**:

  * Create `forge/internal/testing/db_helper.go` (or similar) to manage the local PostgreSQL connection.

  * Configure it to use the provided credentials (`postgres`/`123`).

  * Implement a `CleanDB` function to reset the database state between tests.

* **Implement Fixture System**:

  * Create `forge/internal/testing/fixtures.go` to easily generate complex test data (Users, Products, Categories with relationships).

### 3. Test Suite Optimization

* **Migrate Existing Tests**:

  * Update `forge/orm/integration_test.go` to use the new PostgreSQL test helper instead of in-memory SQLite.

  * Update `forge/db/migrate/execute/recover_test.go` to optionally run against Postgres.

* **Add New Tests**:

  * **Schema Evolution**: Test applying sequential migrations and verifying the DB schema matches.

  * **Complex Relationships**: Test ORM handling of Foreign Keys (e.g., `Category` -> `Product`).

  * **Migration Rollback**: Verify that `DownSQL` works correctly and restores the previous state.

  * **Generated Code**: Verify that code generated by `forge/codegen` works as expected with the ORM.

### 4. Verification

* **Run Test Suite**: Execute all database-related tests.

* **Validate Components**: Ensure ORM, Migrations, and Generated Code work cohesively.

* **Report**: Generate a summary of the verification results.

### Execution Steps

1. **Refactor ORM Manager**: Clean up `forge/orm/manager.go`.
2. **Setup Postgres Test Helper**: Create the testing infrastructure.
3. **Update Integration Tests**: Switch to Postgres and add new test cases.
4. **Verify & Report**: Run tests and document findings.

also find other stuff that need refactore, features that need implementation, writing tests if needed.

test all sorts of nested relationships and make sure our orm handled it all

orm must be type safe and dynamic at the same time

proper error handling on everything.
