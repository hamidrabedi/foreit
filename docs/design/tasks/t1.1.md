TASK: Rewrite the CSS token layer of the Forge admin UI.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to rewrite: src/index.css  (this is the ONLY file you may modify)

STEP 1 — Read the spec:
  /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md
Read "Appendix A" in full. It contains the exact token values. Copy them VERBATIM.
Do not invent, round, or substitute any value.

STEP 2 — Rewrite src/index.css with this exact structure:

1. Keep the first two lines exactly as they are today:
     @import "tailwindcss";
     @config "../tailwind.config.js";

2. @layer base { :root { ... } }  containing, in order:
   - all Light theme values from Appendix A.2
   - all theme-independent values from Appendix A.4
   - the COMPATIBILITY ALIAS block from Appendix A.1, written like this:
       --background: var(--surface-1);
       --card: var(--surface-2);
       --card-foreground: var(--foreground);
       --popover: var(--surface-3);
       --popover-foreground: var(--foreground);
       --secondary: var(--surface-sunken);
       --secondary-foreground: var(--foreground);
       --muted: var(--surface-sunken);
       --accent: var(--surface-sunken);
       --accent-foreground: var(--foreground);
       --destructive: var(--danger);
       --destructive-foreground: var(--danger-fg);
       --input: var(--border);
   - keep these two static defaults (the theme store overwrites them at runtime):
       --primary: 243 75% 58%;
       --primary-foreground: 240 100% 99%;
       --ring: 243 75% 58%;

3. @layer base { .dark { ... } } containing all Dark theme values from Appendix A.3,
   plus the dark overrides:
       --primary: 244 80% 70%;
       --primary-foreground: 245 50% 12%;
       --ring: 244 80% 70%;
   The alias block does NOT need repeating in .dark because the aliases use var()
   and will resolve to the dark values automatically.

4. @media (max-width: 640px) { :root { --page-gutter: 1rem; } }

5. @layer base with:
   - * { @apply border-border; }
   - :focus-visible { outline: 2px solid hsl(var(--ring)); outline-offset: 2px; }
   - body {
       @apply bg-background text-foreground antialiased;
       font-family: var(--font-sans);
       font-feature-settings: "cv02","cv03","cv04","cv11";
     }
   - Add a utility class .num for mono tabular numerals:
       .num { font-family: var(--font-mono); font-variant-numeric: tabular-nums; }

6. KEEP the existing @media (prefers-reduced-motion: reduce) block EXACTLY as it is
   today. Copy it from the current file unchanged.

STEP 3 — DELETE these three classes entirely. They must not appear anywhere in the
new file: .glass-lite, .glass-thick, .premium-shadow
(The whole `@layer components { ... }` block that holds them is removed.)

HARD CONSTRAINTS:
- Modify ONLY src/index.css. Do not touch any other file.
- Do not run any git command.
- Colors are bare HSL triplets like `240 10% 9.5%` with NO hsl() wrapper and NO commas.
- Do not delete the two @import/@config lines at the top.

STEP 4 — Verify your work by running, in the working directory:
    npx tsc -b
It must print no errors. Then report what you changed.
