TASK: Expose the new CSS design tokens to Tailwind.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to modify: tailwind.config.js  (this is the ONLY file you may modify)

STEP 1 — Read these two files first:
  /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md   (read "Appendix A", especially A.5 and section 2.2)
  src/index.css   (the tokens already exist there; you are only exposing them)

STEP 2 — Edit tailwind.config.js, inside `theme.extend`.

KEEP every existing color entry exactly as it is (border, input, ring, background,
foreground, primary, secondary, destructive, muted, accent, popover, card).
You are ADDING to them, not replacing them.

ADD to `theme.extend.colors`:
    "surface-1": "hsl(var(--surface-1))",
    "surface-2": "hsl(var(--surface-2))",
    "surface-3": "hsl(var(--surface-3))",
    "surface-sunken": "hsl(var(--surface-sunken))",
    "border-subtle": "hsl(var(--border-subtle))",
    "border-strong": "hsl(var(--border-strong))",
    "grid-line": "hsl(var(--grid-line))",
    success: { DEFAULT: "hsl(var(--success))", fg: "hsl(var(--success-fg))", surface: "hsl(var(--success-surface))" },
    warning: { DEFAULT: "hsl(var(--warning))", fg: "hsl(var(--warning-fg))", surface: "hsl(var(--warning-surface))" },
    danger:  { DEFAULT: "hsl(var(--danger))",  fg: "hsl(var(--danger-fg))",  surface: "hsl(var(--danger-surface))" },
    info:    { DEFAULT: "hsl(var(--info))",    fg: "hsl(var(--info-fg))",    surface: "hsl(var(--info-surface))" },
    neutral: { DEFAULT: "hsl(var(--neutral))", fg: "hsl(var(--neutral-fg))", surface: "hsl(var(--neutral-surface))" },
    chart: {
      1: "hsl(var(--chart-1))", 2: "hsl(var(--chart-2))", 3: "hsl(var(--chart-3))",
      4: "hsl(var(--chart-4))", 5: "hsl(var(--chart-5))", 6: "hsl(var(--chart-6))",
    },

ADD `theme.extend.fontFamily`:
    sans: ["var(--font-sans)"],
    mono: ["var(--font-mono)"],

ADD `theme.extend.fontSize` — each entry is [size, { lineHeight, letterSpacing, fontWeight }]:
    micro:      ["var(--text-micro)",     { lineHeight: "1.4",  letterSpacing: "0.08em",  fontWeight: "600" }],
    meta:       ["var(--text-meta)",      { lineHeight: "1.45", letterSpacing: "0" }],
    body:       ["var(--text-body)",      { lineHeight: "1.45", letterSpacing: "0" }],
    ui:         ["var(--text-ui)",        { lineHeight: "1.45", letterSpacing: "0" }],
    lead:       ["var(--text-lead)",      { lineHeight: "1.35", letterSpacing: "-0.01em", fontWeight: "600" }],
    title:      ["var(--text-title)",     { lineHeight: "1.2",  letterSpacing: "-0.02em", fontWeight: "600" }],
    display:    ["var(--text-display)",   { lineHeight: "1.2",  letterSpacing: "-0.02em", fontWeight: "600" }],
    metric:     ["var(--text-metric)",    { lineHeight: "1",    letterSpacing: "-0.03em", fontWeight: "700" }],
    "metric-xl":["var(--text-metric-xl)", { lineHeight: "1",    letterSpacing: "-0.03em", fontWeight: "700" }],

ADD `theme.extend.spacing`:
    command: "var(--row-command)",
    grid: "var(--row-grid)",
    "grid-comfy": "var(--row-grid-comfy)",
    "pad-command": "var(--pad-command)",
    "pad-grid": "var(--pad-grid)",
    canvas: "var(--pad-canvas)",
    section: "var(--space-section)",
    gutter: "var(--page-gutter)",

REPLACE the existing `theme.extend.borderRadius` with:
    sm: "var(--radius-sm)",
    DEFAULT: "var(--radius)",
    md: "var(--radius)",
    lg: "var(--radius-lg)",

ADD `theme.extend.boxShadow`:
    overlay: "var(--elev-overlay)",
    dialog: "var(--elev-dialog)",

ADD `theme.extend.transitionTimingFunction`:
    out: "var(--ease-out)",
    "in-out": "var(--ease-in-out)",

ADD `theme.extend.transitionDuration`:
    fast: "var(--dur-fast)",
    base: "var(--dur-base)",
    slow: "var(--dur-slow)",

ADD `theme.extend.maxWidth`:
    page: "var(--page-max)",

HARD CONSTRAINTS:
- Modify ONLY tailwind.config.js. Do not touch any other file.
- Do not remove or rename any existing key.
- Do not run any git command.
- Keep the file valid ES module JavaScript with the existing `export default` shape.

STEP 3 — Verify by running in the working directory:
    npx tsc -b
    npx vite build
Both must succeed. Report what you added.
