TASK: Self-host the two web fonts for the Forge admin UI.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web

CONTEXT: src/index.css declares --font-sans with "Inter" and --font-mono with
"JetBrains Mono", but NEITHER font is actually loaded anywhere in the project. There
is no @fontsource package and no <link> tag. The app silently falls back to system
fonts. Your job is to actually load them, self-hosted (no CDN, no Google Fonts link —
the project's CSP forbids external stylesheets).

STEP 1 — Install the font packages. Run in the working directory:
    npm install @fontsource/inter @fontsource/jetbrains-mono

STEP 2 — Import only the weights we use, in src/main.tsx.
Add these imports directly ABOVE the existing `import './index.css';` line:

    import '@fontsource/inter/400.css';
    import '@fontsource/inter/500.css';
    import '@fontsource/inter/600.css';
    import '@fontsource/inter/700.css';
    import '@fontsource/jetbrains-mono/400.css';
    import '@fontsource/jetbrains-mono/700.css';

Order matters: the font imports must come BEFORE './index.css'.

STEP 3 — Do NOT change the --font-sans / --font-mono declarations in src/index.css.
They are already correct. Do not add any <link> tag to index.html.

HARD CONSTRAINTS:
- Modify ONLY: package.json, package-lock.json (via npm), and src/main.tsx.
- Do not touch src/index.css, tailwind.config.js, or any component.
- Do not run any git command.
- Do not add any external CDN URL or Google Fonts link anywhere.

STEP 4 — Verify by running in the working directory:
    npx tsc -b
    npx vite build
Both must succeed. Report the installed versions and what you changed.
