TASK: Create the PageHeader component for the Forge admin UI.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to CREATE: src/components/ui/page-header.tsx   (the ONLY file you may create or modify)

Read /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md section 7 first.

Follow the exact conventions of the existing file src/components/ui/card.tsx:
React.forwardRef, the `cn` helper imported from "@/lib/utils", named exports,
displayName set.

Write EXACTLY this component:

```tsx
import * as React from "react"
import { cn } from "@/lib/utils"

export interface PageHeaderProps extends React.HTMLAttributes<HTMLElement> {
  /** Small uppercase label above the title, e.g. the model group or section. */
  eyebrow?: React.ReactNode
  title: React.ReactNode
  description?: React.ReactNode
  /** Buttons rendered at the top right. */
  actions?: React.ReactNode
  /** Secondary facts rendered under the header, e.g. counts, last-updated. */
  meta?: React.ReactNode
}
```

Render this structure, using these class strings VERBATIM:

- root element: `<header>` with
  cn("flex flex-col gap-3 border-b border-border-subtle pb-canvas", className)
- inner row: "flex items-start justify-between gap-canvas"
- text column: "flex min-w-0 flex-col gap-1.5"
  - eyebrow (only when provided): <p className="text-micro text-muted-foreground">
  - title: <h1 className="truncate text-title text-foreground">
  - description (only when provided): <p className="max-w-2xl text-ui text-muted-foreground">
- actions (only when provided): <div className="flex shrink-0 items-center gap-2">
- meta (only when provided), as a sibling AFTER the inner row:
  <div className="flex flex-wrap items-center gap-x-4 gap-y-1 text-meta text-muted-foreground">

Spread `...props` onto the root <header>. Forward the ref to the <header>.
Export as: export { PageHeader }

HARD CONSTRAINTS:
- Create ONLY src/components/ui/page-header.tsx. Do not modify any other file.
- Do not wire it into any page yet. That is a separate task.
- Do not run any git command.
- Use ONLY the class names given above. Do not add colors, shadows, or Tailwind
  palette colors (no emerald-500, no zinc-800, no raw hex).

VERIFY: run `npx tsc -b` in the working directory. It must report no errors.
