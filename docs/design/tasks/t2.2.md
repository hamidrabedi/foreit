TASK: Create the StatusBadge component for the Forge admin UI.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to CREATE: src/components/ui/status-badge.tsx  (the ONLY file you may create or modify)

Read /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md sections 3.4 and 7.

PURPOSE: every status pill in the app must use a SEMANTIC tone token, never a raw
Tailwind palette color. Today components hardcode emerald-500 / rose-500 / amber-500.
This component replaces all of that.

Use `cva` from "class-variance-authority" exactly the way src/components/ui/button.tsx
already does. Import `cn` from "@/lib/utils".

Base classes (VERBATIM):
  "inline-flex items-center gap-1.5 rounded-sm border px-1.5 py-0.5 text-micro whitespace-nowrap"

Variant group named `tone`, with these six options and these exact class strings:
  neutral: "border-neutral/20 bg-neutral-surface text-neutral"
  success: "border-success/20 bg-success-surface text-success"
  warning: "border-warning/20 bg-warning-surface text-warning"
  danger:  "border-danger/20  bg-danger-surface  text-danger"
  info:    "border-info/20    bg-info-surface    text-info"
  accent:  "border-primary/20 bg-primary/10      text-primary"

defaultVariants: { tone: "neutral" }

Props interface:
```tsx
export interface StatusBadgeProps
  extends React.HTMLAttributes<HTMLSpanElement>,
    VariantProps<typeof statusBadgeVariants> {
  /** Render a small leading dot in the current text color. */
  dot?: boolean
}
```

When `dot` is true, render as the FIRST child:
  <span aria-hidden className="h-1.5 w-1.5 rounded-full bg-current" />

Root element is a <span>, forwardRef to it, spread ...props, displayName set.
Export: export { StatusBadge, statusBadgeVariants }

HARD CONSTRAINTS:
- Create ONLY src/components/ui/status-badge.tsx. Do not modify any other file.
- Do not migrate existing components to it yet. That is a separate task.
- Do not run any git command.
- No raw hex colors. No Tailwind palette colors (emerald-500, rose-500, amber-500, etc).

VERIFY: run `npx tsc -b` in the working directory. It must report no errors.
