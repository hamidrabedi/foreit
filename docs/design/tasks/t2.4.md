TASK: Add the Tooltip primitive to the Forge admin UI.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may touch: package.json, package-lock.json, src/components/ui/tooltip.tsx
                     and src/main.tsx (one provider line only)

Read /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md sections 5, 6 and 7.

STEP 1 — @radix-ui/react-tooltip is ALREADY INSTALLED. Do NOT run npm install.

STEP 2 — CREATE src/components/ui/tooltip.tsx.
Follow the exact structural conventions of the existing src/components/ui/dialog.tsx
(namespace import as TooltipPrimitive, forwardRef wrappers, cn from "@/lib/utils",
displayName taken from the primitive).

Export: TooltipProvider, Tooltip, TooltipTrigger, TooltipContent

TooltipContent must use these classes VERBATIM:
  cn(
    "z-50 overflow-hidden rounded-sm border border-border bg-surface-3 px-2 py-1 text-meta text-foreground shadow-overlay",
    "animate-in fade-in-0 zoom-in-95 data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95",
    "data-[side=bottom]:slide-in-from-top-1 data-[side=left]:slide-in-from-right-1 data-[side=right]:slide-in-from-left-1 data-[side=top]:slide-in-from-bottom-1",
    className
  )
and default sideOffset={6}.

STEP 3 — In src/main.tsx, wrap the existing <RouterProvider .../> with
<TooltipProvider delayDuration={300} skipDelayDuration={150}> ... </TooltipProvider>,
importing TooltipProvider from "./components/ui/tooltip".
Change NOTHING else in main.tsx.

HARD CONSTRAINTS:
- Do NOT go through the app replacing title= attributes. That is a separate task.
- Do not modify any other component file.
- Do not run any git command.
- No raw hex colors, no Tailwind palette colors.

VERIFY: run `npx tsc -b` and `npx vitest run` in the working directory. Both must pass.
