TASK: Split AdminLayout.tsx — defect VB-12.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files: src/components/layout/AdminLayout.tsx (MODIFY) + NEW files in src/components/layout/

The repo rule is 800 lines max. AdminLayout.tsx is ~840.

=== THIS IS A PURE MECHANICAL EXTRACTION ===
Behaviour, markup, classNames and data-testids must be IDENTICAL afterwards.
You are MOVING code, not redesigning it. Do not "improve" anything.

=== EXTRACT, in this order ===
1. `SidebarItem` (already at module scope, plus its `SidebarItemProps` interface)
   -> src/components/layout/SidebarItem.tsx
2. The module-scope nav helpers `normalizeAdminPath`, `isEntryMatch`,
   `isMenuEntryActive`, `findActiveMenuEntry`
   -> src/components/layout/nav-utils.ts
   (SidebarItem.tsx imports what it needs from here.)
3. The entire <aside> sidebar — brand header, Dashboard link, Pinned section,
   Content Models section with its filter input and groups, Plugins section, logout
   -> src/components/layout/Sidebar.tsx
4. The entire <header> top bar — mobile menu button, GlobalSearch, plugin breadcrumb,
   shortcuts button, ThemeCustomizer, notifications, user chip
   -> src/components/layout/TopBar.tsx

=== PROP CONTRACTS ===
Sidebar and TopBar receive everything via explicit props. NO context, NO globals.
Sidebar needs (at least): models, groupByModel, pluginSections, pinnedModels,
togglePinnedModel, modelFilter, setModelFilter, expandedSections, setExpandedSections,
sidebarOpen, setSidebarOpen, sidebarCompact, setSidebarCompact, pathname, go,
onLogout, logoutPending, prefersReducedMotion.
TopBar needs: models, userName, userRole, userInitials, onOpenMobileNav, onShowHelp,
showPluginHeader, pluginLabel, entryLabel.
Adjust to what the code ACTUALLY uses — do not invent props that are unused.

AdminLayout keeps: all useState/useEffect/useMemo state, the auth redirect, the
`go` callback, `groupByModel`, `pluginSections`, the active-section effects, and
renders <Sidebar/>, <TopBar/>, the content area, and <ShortcutHelpDialog/>.

=== HARD CONSTRAINTS ===
- Do NOT define a component inside another component's render body — this file already
  had that exact bug (VB-11) and it must not come back.
- Keep every data-testid, id, aria-*, className and the framer-motion `layoutId`
  ("admin-nav-indicator") byte-identical. The shared layoutId must still be shared
  across Sidebar's indicators, so keep the <LayoutGroup> wrapper with the nav.
- Do NOT change any route, navigation target, or localStorage key.
- Do not run any git command. Do not run npm install.
- Do NOT introduce any Tailwind palette color, raw hex, or arbitrary font size —
  eslint now errors on those.

=== TARGET ===
AdminLayout.tsx under 250 lines. No new file over 400 lines.

=== VERIFY (use ./node_modules/.bin/, NOT npx — a wrapper swallows npx output) ===
    ./node_modules/.bin/tsc -b
    ./node_modules/.bin/vitest run        (36 tests must pass)
    ./node_modules/.bin/eslint src/components/layout/    (0 errors)
Then report `wc -l` for every file you created or modified.
