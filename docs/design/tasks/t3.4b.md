TASK: Swap GlobalSearch internals to `cmdk` (deferred task from Appendix C).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to modify: src/components/layout/GlobalSearch.tsx  (the ONLY source file you may modify)

STEP 0 — install: npm install cmdk

Read Appendix C of /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md.

=== THE SAFETY NET — READ THIS FIRST ===
src/components/layout/GlobalSearch.test.tsx has FOUR tests that MUST keep passing,
unmodified. You may not edit that test file. They assert:
  1. clicking `data-testid="global-search-trigger"` opens a `role="dialog"` whose
     accessible name is "Global search", showing `data-testid="global-search-input"`,
     and listing model names ("Products", "Orders")
  2. typing into `global-search-input` filters the list (non-matching models disappear
     from the DOM)
  3. record results from the API render, and Enter navigates to the highlighted one
  4. Escape closes the dialog

cmdk does its own filtering. Assertion 2 requires non-matching items to be REMOVED
from the DOM — cmdk does that, but ONLY if you let it filter. If you keep the existing
manual filtering AND cmdk's, items may double-filter or not filter at all. Pick ONE:
prefer keeping the EXISTING manual filtering logic and passing
`shouldFilter={false}` to `<Command>`, because that guarantees the current, tested
behaviour is preserved. Do not re-implement the filtering.

=== WHAT TO CHANGE ===
Replace the hand-rolled list/keyboard/highlight machinery with cmdk primitives:
  <Command shouldFilter={false}>            (wrapping the dialog content)
    <Command.Input data-testid="global-search-input" value={query} onValueChange={setQuery} />
    <Command.List>
      <Command.Empty>...</Command.Empty>
      <Command.Group heading="Models"> <Command.Item .../> </Command.Group>
      <Command.Group heading="Records"> <Command.Item .../> </Command.Group>
    </Command.List>
  </Command>

KEEP unchanged: the trigger button and its data-testid, the dialog wrapper and its
role/aria-label ("Global search"), the data fetching, the navigation targets, the
verbose-name lookup (`modelLabelByName`), and all existing token classNames.
Group headings use `text-micro text-muted-foreground`.

`Command.Input` must remain a real `<input>` that `fireEvent.change` drives — pass
`value`/`onValueChange` so it stays controlled by the existing `query` state.

=== HARD CONSTRAINTS ===
- Do NOT edit GlobalSearch.test.tsx or any other test.
- Do NOT change the accessible name of the dialog.
- NO Tailwind palette colors, NO raw hex, NO arbitrary font sizes (eslint errors).
- Do not run any git command.

=== IF YOU CANNOT KEEP ALL FOUR TESTS GREEN ===
STOP, revert your changes to this file, and report exactly which test broke and why.
Do NOT weaken or skip a test to make the suite pass. A working hand-rolled palette is
better than a broken cmdk one.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
    ./node_modules/.bin/tsc -b
    ./node_modules/.bin/vitest run src/components/layout/GlobalSearch.test.tsx
    ./node_modules/.bin/vitest run        (all 36 tests)
    ./node_modules/.bin/eslint src/components/layout/GlobalSearch.tsx
