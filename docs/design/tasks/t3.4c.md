TASK: Move jsdom test polyfills out of production source (cleanup after the cmdk swap).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY these two:
  src/components/layout/GlobalSearch.tsx
  src/test/setup.ts

=== THE PROBLEM ===
The cmdk migration put jsdom-only polyfills at module scope in a PRODUCTION component.
Look at src/components/layout/GlobalSearch.tsx lines 12-24. It contains:

  // cmdk relies on ResizeObserver and scrollIntoView which are unavailable in jsdom (vitest).
  if (typeof globalThis !== "undefined" && !(globalThis as any).ResizeObserver) { ... }
  if (typeof Element !== "undefined" && !(Element.prototype as any).scrollIntoView) { ... }

This is wrong. Test shims must never ship in the browser bundle, and a module-scope
side effect in a component file runs on every import.

=== THE FIX ===
1. DELETE that entire block (the comment and both `if` statements) from
   src/components/layout/GlobalSearch.tsx. Delete nothing else in that file.
2. ADD the equivalent polyfills to src/test/setup.ts instead — that file is the
   vitest `setupFiles` entry (see vitest.config.ts:10) and runs before every test.
   Read the existing file first and append in its existing style.

=== ACCEPTANCE ===
- `grep -n "ResizeObserver" src/components/layout/GlobalSearch.tsx` returns NOTHING.
- `grep -n "ResizeObserver" src/test/setup.ts` returns the new polyfill.
- The four GlobalSearch tests still pass.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run src/components/layout/GlobalSearch.test.tsx    (4 pass)
  ./node_modules/.bin/vitest run                                                (36 pass, 1 skipped)
  ./node_modules/.bin/eslint src/components/layout/GlobalSearch.tsx src/test/setup.ts  (0 errors)

If tsc reports an error in a file you did NOT edit, IGNORE it — another agent is
editing that file concurrently. Only fix errors in your own two files.

=== HARD CONSTRAINTS ===
- Do not run any git command. Do not run npm install.
- Do not change any behaviour or markup of GlobalSearch.
