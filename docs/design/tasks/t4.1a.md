TASK: Extract the filter panel out of ModelListPage into its own component (task 4.1a).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY these:
  src/pages/ModelListPage.tsx        (edit)
  src/components/list/ListFilterPanel.tsx   (CREATE — the directory may not exist yet)

=== WHY ===
src/pages/ModelListPage.tsx is 1343 lines. The project's own rule caps a file at 800.
This is the first of several mechanical extractions. Behaviour must not change at all.

=== WHAT TO EXTRACT ===
In src/pages/ModelListPage.tsx, the block that begins at the comment

    {/* Expansible Filter Panel */}
    {isFilterOpen && metadata.filters.length > 0 && (

and ends with the matching `)}` about 170 lines later, immediately BEFORE the

    <div
      data-testid="bulk-toolbar"

element. Read the file and confirm the exact boundaries yourself before cutting —
do not trust these line numbers blindly.

=== THE ONLY VALUES THAT BLOCK USES ===
Verified by grep. The extracted JSX references exactly these four things from the
page's scope:
  - activeFilters        (state object)
  - setActiveFilters     (state setter)
  - resetPage            (() => void)
  - metadata.filters     (FilterMetadata[])
plus `isFilterOpen`, which stays in the page as the render guard.

=== HOW ===
1. Create src/components/list/ListFilterPanel.tsx exporting a named component:

     import type { FilterMetadata } from "../../api/types";

     type ListFilterPanelProps = {
       filters: FilterMetadata[];
       activeFilters: Record<string, any>;
       setActiveFilters: React.Dispatch<React.SetStateAction<Record<string, any>>>;
       resetPage: () => void;
     };

     export function ListFilterPanel({ ... }: ListFilterPanelProps) { ... }

   Match the exact prop/state types already used in ModelListPage — read the
   `activeFilters` useState declaration there and mirror it. Do NOT introduce `any`
   where the page already has a concrete type.

2. Move the JSX in verbatim. Inside the new file, `metadata.filters` becomes the
   `filters` prop. Everything else keeps its current name.

3. Move over ONLY the imports that block actually needs (Select/SelectItem/etc.,
   Input, Button, Label, X icon, cn — whatever it genuinely references).

4. In ModelListPage.tsx, replace the removed block with:

     {isFilterOpen && metadata.filters.length > 0 && (
       <ListFilterPanel
         filters={metadata.filters}
         activeFilters={activeFilters}
         setActiveFilters={setActiveFilters}
         resetPage={resetPage}
       />
     )}

   and add the import. Then DELETE from ModelListPage.tsx any import that is now
   unused — tsc/eslint will tell you which. Do not delete an import that is still
   used elsewhere in the page.

=== ACCEPTANCE ===
- `wc -l src/pages/ModelListPage.tsx` is roughly 170 lines SMALLER than 1343.
- Every `data-testid` that was inside the filter panel still exists, unchanged,
  in the new file. Do not rename or drop a single one.
- No visual or behavioural change whatsoever. This is a pure move.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run          (36 pass, 1 skipped — ModelListPage.test.tsx MUST stay green)
  ./node_modules/.bin/eslint src/pages/ModelListPage.tsx src/components/list/ListFilterPanel.tsx
                                          (0 errors)

If tsc reports an error in a file you did NOT edit, IGNORE it — another agent is
editing that file concurrently. Only fix errors in your own two files.

=== HARD CONSTRAINTS ===
- Do not run any git command. Do not run npm install.
- No Tailwind palette colors, no raw hex, no arbitrary font sizes (these are eslint errors).
- Do not "improve" the JSX while moving it. Verbatim move only.
