TASK: Extract the bulk-action toolbar out of ModelListPage into its own component (task 4.1b).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY these:
  src/pages/ModelListPage.tsx               (edit)
  src/components/list/ListBulkToolbar.tsx   (CREATE)

=== WHY ===
src/pages/ModelListPage.tsx is still over the project's 800-line file cap. This is a
pure mechanical extraction. Behaviour and markup must not change at all.

=== WHAT TO EXTRACT ===
Find the element in src/pages/ModelListPage.tsx that opens with:

    <div
      data-testid="bulk-toolbar"

and extract that whole element, through its matching closing `</div>`. Locate the
boundaries by reading the file and matching the JSX nesting — do not guess.

=== HOW ===
1. Read the extracted block and list every identifier it references from the page's
   scope. Expect roughly: `hasSelection`, `selectedIds`, `setSelectedIds`,
   `metadata.actions`, `metadata.permissions`, `handleActionClick`, `actionLoading`,
   `setBulkDeleteConfirmOpen`, `bulkDeleteMutation`, and `cn`. Verify the real list
   yourself — do not trust this one.

2. Create src/components/list/ListBulkToolbar.tsx exporting a named component
   `ListBulkToolbar` whose props are exactly those identifiers (minus `cn`, which is
   an import). Use the concrete types already present in ModelListPage — import
   `ActionMetadata` / `PermissionMetadata` from `../../api/types` rather than
   widening anything to `any`. Pass `bulkDeleteMutation.isPending` as a plain
   `boolean` prop named `bulkDeletePending` instead of passing the mutation object.

3. Move the JSX in VERBATIM. Do not restyle, reorder, or "clean up" anything.

4. In ModelListPage.tsx replace the block with `<ListBulkToolbar ... />`, add the
   import, and delete any import that is now unused (eslint will name them).

=== ACCEPTANCE ===
- Every `data-testid` inside that toolbar still exists, byte-identical, in the new
  file: `bulk-toolbar`, `bulk-cancel`, `bulk-delete-button`, and every
  `bulk-action-${action.name}`. ModelListPage.test.tsx asserts on these.
- `wc -l src/pages/ModelListPage.tsx` drops by roughly 70 lines.
- No visual or behavioural change.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run          (36 pass, 1 skipped)
  ./node_modules/.bin/eslint src/pages/ModelListPage.tsx src/components/list/ListBulkToolbar.tsx
                                          (0 errors)

If tsc reports an error in a file you did NOT edit, IGNORE it — another agent may be
editing that file concurrently. Only fix errors in your own two files.

=== HARD CONSTRAINTS ===
- Do not run any git command. Do not run npm install.
- No Tailwind palette colors, no raw hex, no arbitrary font sizes (eslint errors).
- Verbatim move only.
