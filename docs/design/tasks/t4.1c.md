TASK: Extract the table cell renderer out of ModelListPage into its own component (task 4.1c).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY these:
  src/pages/ModelListPage.tsx           (edit)
  src/components/list/ListCell.tsx      (CREATE)

=== WHY ===
src/pages/ModelListPage.tsx is over the project's 800-line file cap, and the cell
renderer is the densest ~100 lines in it. Extracting it makes both files reviewable.
This is a pure mechanical extraction — behaviour and markup must not change.

=== WHAT TO EXTRACT ===
Inside the `<TableBody>`, find the row map:

    objects.map((obj: any) => {
      const isSelected = selectedIds.includes(obj.id);
      ...

Within it there is an inner map over the display columns that begins:

    {displayFields.map((fieldName, colIdx) => {
      const field = fieldsByName.get(fieldName);
      const val = obj[fieldName];
      ...
      return (
        <TableCell ... >
          ...
        </TableCell>
      );
    })}

Extract the ENTIRE body of that inner callback — the local `const` declarations AND
the returned `<TableCell>` — into a component. Keep the `.map()` itself in the page.
Read the file and match the JSX nesting to find the exact boundaries; do not guess
line numbers.

=== HOW ===
1. Create src/components/list/ListCell.tsx exporting `ListCell`.
   Its props are exactly what the callback body reads from the enclosing scope.
   Expect roughly:

     type ListCellProps = {
       fieldName: string;
       colIdx: number;
       obj: Record<string, any>;
       field: FieldMetadata | undefined;
       relation: RelationMetadata | undefined;
       navigate: ReturnType<typeof useNavigate>;
     };

   Verify the real list yourself. Note `fieldsByName.get(fieldName)` and
   `relationByField.get(fieldName)` are BOTH resolved in the page and passed IN as
   `field` and `relation` — do not pass the Maps.

   Import `FieldMetadata` and `RelationMetadata` from `../../api/types`. Do not widen
   anything to `any` that currently has a type.

2. Move the locals (`isPrimary`, `isDate`, `matchedChoice`, `isEmpty`, and any
   others) into the component body unchanged, and move the returned `<TableCell>`
   in verbatim.

3. In ModelListPage.tsx the inner map becomes:

     {displayFields.map((fieldName, colIdx) => (
       <ListCell
         key={fieldName}
         fieldName={fieldName}
         colIdx={colIdx}
         obj={obj}
         field={fieldsByName.get(fieldName)}
         relation={relationByField.get(fieldName)}
         navigate={navigate}
       />
     ))}

   Keep the `key` on the same element it is on today. Add the import; delete any
   import in ModelListPage that is now unused (eslint will name them) — but do NOT
   delete one still used elsewhere in the page.

=== ACCEPTANCE — READ THIS ===
- The FK cell's `data-testid={`fk-${fieldName}-${obj.id}`}` must survive byte-identical.
  ModelListPage.test.tsx asserts on it.
- `<EmptyValue/>` must remain the FIRST branch for an empty value, before the
  boolean / choice / date / FK branches. Do not reorder the branches.
- Sticky-column classes on the first (identity) column must be preserved exactly —
  that column relies on `--sticky-id-offset` set by the ancestor. Changing the class
  string silently breaks horizontal scroll.
- `wc -l src/pages/ModelListPage.tsx` drops by roughly 90 lines.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run          (36 pass, 1 skipped)
  ./node_modules/.bin/eslint src/pages/ModelListPage.tsx src/components/list/ListCell.tsx
                                          (0 errors)

If tsc reports an error in a file you did NOT edit, IGNORE it — another agent may be
editing that file concurrently. Only fix errors in your own two files.

=== IF YOU CANNOT KEEP ALL TESTS GREEN ===
STOP, revert your changes to both files, and report exactly which test broke and why.
Do NOT weaken, skip, or edit a test to make the suite pass.

=== HARD CONSTRAINTS ===
- Do not run any git command. Do not run npm install.
- No Tailwind palette colors, no raw hex, no arbitrary font sizes (eslint errors).
- Verbatim move only. Do not "improve" the branch logic while moving it.
