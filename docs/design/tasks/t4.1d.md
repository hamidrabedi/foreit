TASK 4.1d: extract the list toolbar and pagination bar out of ModelListPage (pure move).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY:
  src/pages/ModelListPage.tsx                 (edit)
  src/components/list/ListToolbar.tsx         (CREATE)
  src/components/list/ListPagination.tsx      (CREATE)

=== WHY ===
ModelListPage.tsx is 1035 lines; the project cap is 800. Filter panel, bulk toolbar and cell
renderer are already extracted into src/components/list/ — read ListFilterPanel.tsx and
ListBulkToolbar.tsx first and follow the same style (named export, explicit typed props,
no `any` where the page has a type).

=== EXTRACTION 1: ListToolbar ===
Inside `<CardHeader ...>`, the FIRST child element:
    <div className="flex flex-col sm:flex-row items-stretch sm:items-center gap-3">
      <div className="relative flex-1 w-full">  ... search input ...
      <div className="flex items-center gap-2 flex-wrap"> ... saved views Select, Save view,
           delete view, Filters button ...
    </div>
It ends immediately before the `{/* Expansible Filter Panel */}` comment. Move that whole
element into ListToolbar. Props = exactly the identifiers it reads, expected roughly:
  searchLabel (metadata.verbose_name_plural), searchInput, onSearchChange (handleSearchChange),
  savedViews, selectedSavedView, onApplySavedView (handleApplySavedView),
  onOpenSaveView (the inline `() => { setSaveViewName(""); setSaveViewOpen(true); }` — keep the
  lambda in the page and pass it), onDeleteSavedView (handleDeleteSavedView),
  deleteViewPending (deleteSavedViewMutation.isPending as boolean),
  isFilterOpen, onToggleFilters (`() => setIsFilterOpen(!isFilterOpen)` defined in the page),
  activeFilterCount.
Verify the real list by reading the JSX; do not pass whole mutation objects or setters that the
JSX does not need.

=== EXTRACTION 2: ListPagination ===
The element right after `</CardContent>` and before `</Card>`:
    <div className="px-6 py-4 bg-muted/30 border-t border-border-subtle flex flex-col lg:flex-row lg:items-center gap-4">
      ... pagination-status, page-size Select, Page X of Y, Previous/Next ...
    </div>
Props expected roughly: from, to, totalCount, page, totalPages, pageSize (effectivePageSize),
pageSizeOptions, onPageSizeChange (a page-level lambda doing setPageSize(Number(v)); resetPage()),
onPrev, onNext (page-level lambdas wrapping setPage). Verify by reading.

=== RULES ===
- Move JSX VERBATIM: same classNames, same text, same aria attributes, same data-testids:
  search-input, clear-search, saved-view-select, save-view-button, delete-view-button,
  filter-button, pagination-status, page-size-select, pagination-prev, pagination-next.
- Keep behaviour identical. Radix Select sentinel "__none__" logic moves as-is.
- Remove imports from ModelListPage that become unused; do not remove ones still used.

=== ACCEPTANCE ===
- `wc -l src/pages/ModelListPage.tsx` is under 820.
- Total count of `data-testid` occurrences across ModelListPage.tsx + all files in
  src/components/list/ is unchanged (it is 26 in ModelListPage + list components today — count
  before you start with: cat src/pages/ModelListPage.tsx src/components/list/*.tsx | grep -o data-testid | wc -l).

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run          (36 passed, 1 skipped)
  ./node_modules/.bin/eslint src/pages/ModelListPage.tsx src/components/list/   (0 errors)
If tsc reports an error only in src/routes/* ignore it — another agent is editing routes.

=== HARD CONSTRAINTS ===
- NO git commands. No npm install.
- No Tailwind palette colors, no raw hex, no arbitrary font sizes (eslint errors).
- Verbatim move only; do not restyle.
- Final report: new line counts, testid count before/after, gate output.
