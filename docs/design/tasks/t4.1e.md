TASK 4.1e: final ModelListPage extractions to get under the 800-line cap (pure move).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY:
  src/pages/ModelListPage.tsx                  (edit)
  src/components/list/ListTableHeader.tsx      (CREATE)
  src/components/list/ListRowActions.tsx       (CREATE)
  src/components/list/SaveViewDialog.tsx       (CREATE)

ModelListPage.tsx is 906 lines; target < 800. Previous extractions (ListToolbar, ListPagination,
ListFilterPanel, ListBulkToolbar, ListCell) are in src/components/list/ — read two of them first and
match their style: named export, explicit typed props, no `any` where the page has a real type.

=== EXTRACTION 1: ListTableHeader ===
The `<TableHeader className="bg-muted/40 sticky top-0"> ... </TableHeader>` element (starts around
line 596, inside `<Table className="tabular-nums">`). Move the whole element. Props = exactly the
identifiers it reads (expect roughly: allSelected, someSelected, a select-all toggle handler,
displayFields, fieldsByName, sortField/sortDir or the sort handler, sortIcon, and any label helper).
Verify by reading. If `sortIcon` is a function defined in the page, pass it as a prop or move it
into the new file (move it if nothing else in the page uses it).

=== EXTRACTION 2: ListRowActions ===
Inside the row map in `<TableBody>`, the LAST `<TableCell className="text-right pr-4">` containing
the view / edit / delete icon buttons (data-testids `view-${obj.id}` (check exact), `edit-${obj.id}`,
`delete-${obj.id}`). Move that whole TableCell into ListRowActions. Props: the record id, model name,
permissions (metadata.permissions), navigate, onDelete (`() => setDeleteId(obj.id)` kept as a page
lambda or pass setDeleteId), deletePending (deleteMutation.isPending as boolean). Verify by reading.

=== EXTRACTION 3: SaveViewDialog ===
The block starting at the comment `{/* Save view dialog */}` — the `<Dialog open={saveViewOpen} ...>`
element through its closing `</Dialog>`, just before `{/* Dialogs */}`. Props expected: open,
onOpenChange, name, onNameChange, onConfirm (handleSaveViewConfirm), pending (save mutation
isPending). Verify by reading.

=== RULES ===
- Move JSX VERBATIM: same classNames, text, aria attributes, and every data-testid unchanged.
- Remove imports from ModelListPage that become unused; keep ones still used.
- Before starting, record:  cat src/pages/ModelListPage.tsx src/components/list/*.tsx | grep -o data-testid | wc -l
  (it is 36). After, the same command must print 36.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run          (46 passed, 1 skipped)
  ./node_modules/.bin/eslint src/pages/ModelListPage.tsx src/components/list/   (0 errors)
  wc -l src/pages/ModelListPage.tsx       (< 800)

=== HARD CONSTRAINTS ===
- NO git commands. No npm install.
- No palette colors, raw hex, or text-[Npx].
- Verbatim move only.
- Final report: line counts, testid count before/after, gate output.
