TASK 4.2b (VB-02 phase 2, frontend): show related-object labels instead of "#id" in list FK cells.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY:
  src/api/types.ts                      (PaginatedResponse type)
  src/pages/ModelListPage.tsx           (thread the display map down)
  src/components/list/ListCell.tsx      (render the label)
  src/components/list/ListCell.test.tsx (CREATE)

=== BACKEND CONTRACT (additive; may be absent on older backends) ===
The list endpoint response gains an optional field:
    "display": { "<relation field name>": { "<related id as string>": "<human label>" } }
Example: { "results":[{"id":7,"customer_id":91}], "display": {"customer_id": {"91": "Acme GmbH"}} }
The key is the relation name from metadata.relations[].name. Rows are unchanged.

=== TODAY ===
src/components/list/ListCell.tsx renders an FK value as a linked mono `#<id>` button with
data-testid={`fk-${fieldName}-${obj.id}`} that navigates to /$model/$id/view of the related model.
`EmptyValue` is the first branch for empty values.

=== FIX ===
1. src/api/types.ts: add to `PaginatedResponse<T>`:
       display?: Record<string, Record<string, string>>;
2. ModelListPage.tsx: take `display` from the list query data (the same object that provides results /
   count) and pass the per-field map to each ListCell as a new optional prop
   `displayLabels?: Record<string, string>` = `data?.display?.[fieldName]`.
3. ListCell.tsx FK branch:
       const label = displayLabels?.[String(val)];
   - if label is a non-empty string: render the SAME button element (same data-testid, same onClick,
     same focus/hover classes) but its text is the label, using `text-ui` (not font-mono) and
     `truncate max-w-[16rem]` with `title={label}`; keep a small muted `#<id>` after it inside the button
     as `<span className="ml-1.5 font-mono text-meta text-muted-foreground tabular-nums">#{String(val)}</span>`.
   - else: render exactly today's `#<id>` button (fallback for older backends).
   - EmptyValue branch stays first and unchanged.
4. No other visual changes.

=== TESTS (src/components/list/ListCell.test.tsx, vitest + @testing-library/react) ===
Read an existing component test in src/ for render/wrapper/router-mock patterns (ListCell uses a
navigate prop — pass vi.fn()). Render inside <table><tbody><tr> to keep DOM valid.
  - FK value 91 with displayLabels {"91":"Acme GmbH"} -> button text contains "Acme GmbH" and "#91";
    testid fk-<field>-<rowid> present; clicking calls navigate with the related model + id.
  - FK value 91 with no displayLabels -> button text is "#91" only.
  - FK value null -> EmptyValue (em dash) rendered, no button.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run
  ./node_modules/.bin/eslint src/pages/ModelListPage.tsx src/components/list/ src/api/types.ts  (0 errors)
If tsc/build errors appear ONLY in vite.config.ts, ignore them — another agent may be editing it.

=== HARD CONSTRAINTS ===
- NO git commands. No npm install.
- Tokens only: no palette colors, raw hex, or text-[Npx]. (`max-w-[16rem]` spacing arbitrary value is allowed.)
- Final report: files changed, tests, gate output.
