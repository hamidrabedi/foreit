TASK: Create the Select primitive (needed to fix defect VB-13).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to CREATE: src/components/ui/select.tsx   (the ONLY file you may create or modify)

`@radix-ui/react-select` is ALREADY a dependency. Do NOT run npm install.

CONTEXT: the app uses raw unstyled `<select>` elements in the list-page filter panel
even though the Radix select package is installed but has no wrapper component. This
task only creates the wrapper. A separate task swaps the call sites.

Follow the EXACT structural conventions of the existing src/components/ui/dialog.tsx
and src/components/ui/dropdown-menu.tsx in this repo: namespace import as
`SelectPrimitive`, `React.forwardRef` wrappers, `cn` from "@/lib/utils",
`displayName` taken from the primitive.

Export exactly these names:
  Select, SelectGroup, SelectValue, SelectTrigger, SelectContent, SelectLabel,
  SelectItem, SelectSeparator

Use `ChevronDown`, `ChevronUp` and `Check` from "lucide-react".

Class strings — use these VERBATIM:

SelectTrigger:
  "flex h-9 w-full items-center justify-between gap-2 rounded border border-border bg-surface-2 px-2 text-ui text-foreground transition-colors duration-fast ease-out placeholder:text-muted-foreground focus:outline-none focus-visible:ring-2 focus-visible:ring-ring disabled:cursor-not-allowed disabled:opacity-50 [&>span]:line-clamp-1"
  with a trailing <SelectPrimitive.Icon asChild><ChevronDown className="h-4 w-4 shrink-0 text-muted-foreground" /></SelectPrimitive.Icon>

SelectContent (render inside SelectPrimitive.Portal, default position="popper"):
  "relative z-50 max-h-96 min-w-[8rem] overflow-hidden rounded border border-border bg-surface-3 text-foreground shadow-overlay data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95"
  Viewport className: "p-1"
  Include SelectPrimitive.ScrollUpButton and ScrollDownButton with
  "flex cursor-default items-center justify-center py-1 text-muted-foreground"
  containing ChevronUp / ChevronDown at "h-4 w-4".

SelectLabel:
  "px-2 py-1.5 text-micro text-muted-foreground"

SelectItem:
  "relative flex w-full cursor-default select-none items-center rounded-sm py-1.5 pl-7 pr-2 text-ui outline-none transition-colors duration-fast ease-out focus:bg-surface-sunken focus:text-foreground data-[disabled]:pointer-events-none data-[disabled]:opacity-50"
  with the check indicator absolutely positioned:
  <span className="absolute left-2 flex h-3.5 w-3.5 items-center justify-center">
    <SelectPrimitive.ItemIndicator><Check className="h-3.5 w-3.5" /></SelectPrimitive.ItemIndicator>
  </span>

SelectSeparator:
  "-mx-1 my-1 h-px bg-border-subtle"

HARD CONSTRAINTS:
- Create ONLY src/components/ui/select.tsx. Do not modify any other file.
- Do NOT change any page or replace any existing <select> yet — separate task.
- Do not run npm install. Do not run any git command.
- No raw hex, no Tailwind palette colors, no arbitrary font sizes.

VERIFY: run `npx tsc -b` in the working directory. It must report no errors.
