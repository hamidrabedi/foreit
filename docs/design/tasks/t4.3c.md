TASK: Convert the last two raw <select> elements to the Select component (finishes VB-13).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to modify: src/pages/ModelListPage.tsx   (the ONLY file you may modify)

The filter panel was already converted. Two raw `<select>` elements remain, around
lines 579 and 1175: the SAVED VIEWS selector and the PAGE SIZE selector. Convert both
to the `Select` component that is already imported in this file.

For each one:
- Keep the exact same `id`, `data-testid`, `aria-label` and option values.
- Keep the exact same onChange semantics — note Radix uses `onValueChange={(v) => ...}`
  and gives you the VALUE directly, not an event, so `e.target.value` becomes `v`.
- Radix Select forbids an empty-string item value. If an option currently has
  `value=""`, use a `"__none__"` sentinel and map it back to `""` in onValueChange,
  exactly as the filter panel already does with `__all__`.
- Trigger styling: `<SelectTrigger className="h-9 w-auto min-w-[<sensible>] text-ui">`
  preserving whatever width class the original had.
- The page-size selector's numeric options must stay strings in the Select and be
  parsed back with Number() where the handler expects a number.

HARD CONSTRAINTS:
- Modify ONLY src/pages/ModelListPage.tsx.
- Do NOT change pagination maths, saved-view logic, filters, or the table body.
- Do NOT remove or rename any data-testid.
- Do not run any git command.

VERIFY — all must pass:
    npx tsc -b
    npx vitest run      (all 36 tests)
    npx eslint src/pages/ModelListPage.tsx
Then confirm `grep -c "<select" src/pages/ModelListPage.tsx` is 0.
