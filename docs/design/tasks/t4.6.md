TASK: Recompose the Dashboard onto the new design system.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to modify: src/pages/DashboardPage.tsx   (the ONLY file you may modify)

Read /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md — sections 1, 2,
3.4, 4, 7 — before starting.

=== WHY ===
The dashboard currently renders four IDENTICAL stat cards, each with its metric at
`text-2xl` (24px) and a hardcoded Tailwind palette color (`text-emerald-500`,
`text-blue-500`, `text-purple-500`, `text-blue-600 dark:text-blue-400`). The design
system forbids palette colors, and its whole point is scale contrast — metrics belong
at `text-metric` (40px) in the MONO font. Uniform card grids with no hierarchy are
explicitly a banned pattern.

The components to use ALREADY EXIST. Use them; do not write new ones.

=== STEP 1: imports ===
Add:
    import { PageHeader } from "../components/ui/page-header";
    import { StatTile } from "../components/widgets/stat-tile";
Remove the `Card, CardContent, CardDescription, CardHeader, CardTitle` import ONLY if
nothing else in the file still uses those. Check before removing.

=== STEP 2: replace the page's ad-hoc title block with PageHeader ===
Find the existing header region (the block holding the page heading plus the
"Quick Find (⌘K)" and "Design System" buttons) and replace it with:

    <PageHeader
      eyebrow="Overview"
      title="Dashboard"
      description="Registered models, record volume, and installed extensions."
      actions={ /* keep the EXISTING Quick Find and Design System buttons verbatim */ }
    />

Keep both buttons exactly as they are — same routes, same icons, same labels, same
onClick/asChild behaviour. Only their container changes.

=== STEP 3: replace the four stat Cards with StatTile ===
Replace the whole `{/* Executive Stats Cards */}` grid with:

    <div className="grid grid-cols-1 gap-canvas sm:grid-cols-2 lg:grid-cols-4">
      <StatTile
        className="lg:col-span-2 cursor-pointer"
        label="Registered Models"
        value={models.length}
        description="Schema aggregates managed"
        icon={Database}
        role="link"
        tabIndex={0}
        aria-label={`Browse ${models.length} registered models`}
        onClick={() => navigate({ to: "/registry" })}
        onKeyDown={(e: React.KeyboardEvent) => {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            navigate({ to: "/registry" });
          }
        }}
      />
      <StatTile
        label="Total Records"
        value={totalRecords.toLocaleString()}
        description="Persistent database rows"
        icon={Layers}
      />
      <StatTile
        label="Active Plugins"
        value={plugins.length}
        description="Modular extensions loaded"
        icon={Package}
      />
      <StatTile
        className="sm:col-span-2 lg:col-span-4"
        label="Audit Logging"
        value="Active"
        description="Thread-safe memory buffer and storage"
        icon={ShieldCheck}
      />
    </div>

Note the deliberate asymmetry: the first tile spans two columns and the audit tile
spans the full width. That is the intended hierarchy — do NOT make them uniform.
ALL per-icon colors are dropped; StatTile styles its own icon from tokens.

=== STEP 4: purge remaining palette colors and arbitrary sizes ===
Everywhere else in this file, replace:
- `text-emerald-*`, `text-blue-*`, `text-purple-*`, `text-amber-*`, `text-rose-*`,
  `bg-*-500/10`, `border-*-500/30` -> the closest semantic token
  (`text-success`, `text-info`, `text-primary`, `text-muted-foreground`,
   `bg-success-surface`, `border-success/20`, ...)
- `text-xs`  -> `text-meta`
- `text-sm`  -> `text-ui`
- `text-xl` / `text-2xl` on a SECTION HEADING -> `text-display`
- any `text-[Npx]` -> the nearest token class
- `gap-8` between major regions -> `gap-canvas`
- `shadow-sm` / `shadow-md` on a Card -> remove entirely (cards are flat now)
- `border-border/50`, `border-border/60` -> `border-border-subtle`

Section headings inside the page (e.g. "Application Models") get an eyebrow treatment:
a `text-micro text-muted-foreground` label above a `text-display` heading.

=== HARD CONSTRAINTS ===
- Modify ONLY src/pages/DashboardPage.tsx.
- Do NOT change any data fetching, any route, any navigate() target, or any
  existing data-testid.
- Do NOT change the SDUIRoot rendering or the model-search behaviour.
- Do not run any git command. Do not run npm install.
- After the change the file must contain ZERO Tailwind palette colors and ZERO
  arbitrary font sizes.

=== VERIFY ===
Run in the working directory, all must pass:
    npx tsc -b
    npx vitest run
    npx eslint src/pages/DashboardPage.tsx
Then confirm:
    grep -nE "emerald-|blue-|purple-|amber-|rose-|text-\[[0-9]" src/pages/DashboardPage.tsx
returns nothing.
