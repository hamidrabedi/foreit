TASK: Wire PageHeader into ModelListPage and finish its typography pass.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to modify: src/pages/ModelListPage.tsx   (the ONLY file you may modify)

Read /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md sections 2, 4, 7.

NOTE ON PARALLEL WORK: if `tsc -b` reports an error in a file you did NOT edit, IGNORE
it — another agent may be editing that file concurrently. Only fix errors in
ModelListPage.tsx.

=== STEP 1: PageHeader ===
Find the page's ad-hoc heading block (the model title, the record-count line, and the
"Add <model>" / export / column-visibility actions) and replace the heading portion with:

    <PageHeader
      eyebrow={metadata.verbose_name_plural}
      title={metadata.verbose_name_plural}
      description={metadata.description || undefined}
      actions={/* the EXISTING action buttons, verbatim */}
      meta={/* the EXISTING record-count / selection summary line, verbatim */}
    />

Import: import { PageHeader } from "../components/ui/page-header";
Keep every button's route, icon, label, permission gate, onClick and data-testid
EXACTLY as they are. Only the surrounding container changes.
If `eyebrow` and `title` would render the same string, pass eyebrow="Records" instead
so the two lines are not duplicates.

=== STEP 2: typography pass ===
Throughout this file replace:
  text-sm  -> text-ui
  text-xs  -> text-meta
  any remaining text-[Npx] -> the nearest scale class
    (10-11px -> text-micro, 12px -> text-meta, 13px -> text-body, 14px -> text-ui)
  text-lg / text-xl / text-2xl on a heading -> text-title
Any count, id, page number or total gets `font-mono tabular-nums`.

=== STEP 3: surfaces ===
  border-border/50, border-border/60 -> border-border-subtle
  shadow-sm / shadow-md on a card or panel -> remove (surfaces are flat)
  bg-muted/40, bg-muted/50 used as a hover -> bg-surface-sunken/60

=== HARD CONSTRAINTS ===
- Modify ONLY src/pages/ModelListPage.tsx.
- Do NOT change the table cell renderer, filters, sorting, pagination, bulk actions,
  sticky-column classes, or the FK/EmptyValue/StatusBadge logic.
- Do NOT remove or rename any data-testid.
- NO Tailwind palette colors, NO raw hex, NO arbitrary font sizes (eslint errors on these).
- Do not run any git command. Do not run npm install.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
    ./node_modules/.bin/eslint src/pages/ModelListPage.tsx     (0 errors)
    ./node_modules/.bin/vitest run                              (36 tests pass)
    ./node_modules/.bin/tsc -b                                  (ignore errors in files you did not touch)
