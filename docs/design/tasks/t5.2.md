TASK: Split ModelUpsertPage (1389 lines) — defect VB-12.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files: src/pages/ModelUpsertPage.tsx (MODIFY) and NEW files under src/components/form/

The repo rule is 800 lines maximum per file. ModelUpsertPage is 1389.

=== THIS IS A PURE MECHANICAL EXTRACTION ===
Behaviour, markup, classNames and data-testids must be IDENTICAL afterwards. You are
moving code, not redesigning it. Do not "improve" anything while moving it.

=== STEP 1: read the file and identify these seams ===
Look for, and extract in this order:
 a) The field-kind resolution helper(s) — there is a `resolveFieldKind` function with
    its own test at src/pages/resolveFieldKind.test.ts. That test imports it from its
    CURRENT location, so if you move it you MUST update the test's import path.
    Prefer: move it to src/components/form/resolve-field-kind.ts and update the import
    in resolveFieldKind.test.ts (this is the ONE test file you may edit, and ONLY its
    import line).
 b) The single-field renderer (the large function that switches on field kind and
    returns the appropriate input/select/textarea/checkbox widget) ->
    src/components/form/FieldRenderer.tsx
 c) The inline-relation / nested formset section ->
    src/components/form/InlineRelations.tsx

=== STEP 2: extraction rules ===
- Every extracted component takes explicit props. NO module-level mutable state.
- Do NOT define a component inside another component's render body — that causes
  remounting (this repo already had that bug once).
- Keep every `data-testid`, `id`, `aria-*`, `className` and `name` byte-identical.
- Keep the existing react-hook-form wiring: pass `control`, `register`, `errors`,
  `setValue`, `watch` (whichever are actually used) through as props rather than
  reaching for context.
- If TypeScript types are shared, put them in src/components/form/types.ts.

=== STEP 3: target ===
After the split:
- src/pages/ModelUpsertPage.tsx must be UNDER 600 lines.
- No new file may exceed 400 lines.

=== HARD CONSTRAINTS ===
- Do NOT change any behaviour, validation rule, submit handler, or API call.
- Do NOT restyle anything. Class strings move verbatim.
- The ONLY test file you may touch is resolveFieldKind.test.ts, and ONLY its import path.
- Do not run any git command. Do not run npm install.

=== VERIFY ===
Run in the working directory, all must pass:
    npx tsc -b
    npx vitest run
    npx eslint src/pages/ModelUpsertPage.tsx src/components/form/
All 36 tests must still pass. Then report the final line count of every file you
created or modified (`wc -l`).
