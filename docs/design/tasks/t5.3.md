TASK: Recompose ModelViewPage onto the design system.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
File to modify: src/pages/ModelViewPage.tsx   (the ONLY file you may modify)

Read /home/hamid/Other/projects/foreit/docs/design/admin-ui-system.md — sections 2, 3,
4, 7 and Appendix B.

=== STEP 1: use PageHeader ===
Replace the page's ad-hoc title/heading block with:

    <PageHeader
      eyebrow={metadata?.verbose_name}
      title={/* the existing record title/identifier expression, unchanged */}
      description={/* existing subtitle if there is one, else omit */}
      actions={/* keep the EXISTING edit/delete/back buttons verbatim */}
    />

Import: import { PageHeader } from "../components/ui/page-header";
Keep every button's route, icon, label, permission gate and data-testid unchanged.

=== STEP 2: empty values ===
Import { EmptyValue } from "../components/ui/empty-state" and use <EmptyValue /> for
any field whose value is null, undefined or "". Do NOT treat 0 or false as empty —
use an explicit check:  value === null || value === undefined || value === ""

=== STEP 3: foreign keys (VB-02 phase 1, mirroring ModelListPage) ===
Build a relation lookup from metadata.relations, keyed by BOTH `rel.name` and
`${rel.name}_id`, exactly as ModelListPage does. For a field that matches a relation
and has a non-empty value, render a link to the related record instead of a bare id:

    <button
      type="button"
      onClick={() => navigate({ to: "/$model/$id/view", params: { model: relation.related_model, id: String(value) } })}
      className="font-mono text-meta tabular-nums text-muted-foreground transition-colors duration-fast ease-out hover:text-primary hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded"
    >
      #{String(value)}
    </button>

Read how ModelListPage.tsx does this and follow the same shape.

=== STEP 4: typography and surfaces ===
- Field VALUES render at `text-body`; field LABELS at `text-micro text-muted-foreground`.
- Any id, count, timestamp or numeric value gets `font-mono tabular-nums`.
- Booleans and choice/status values use the StatusBadge component
  (src/components/ui/status-badge.tsx, tones success|warning|danger|info|neutral).
- Replace `text-sm` -> `text-ui`, `text-xs` -> `text-meta`.
- Cards get no shadow; use `border-border-subtle` and `bg-surface-2`.

=== HARD CONSTRAINTS ===
- Modify ONLY src/pages/ModelViewPage.tsx.
- Do NOT change data fetching, permissions logic, delete confirmation, or any route.
- Do NOT remove or rename any data-testid.
- NO Tailwind palette colors, NO raw hex, NO arbitrary font sizes — eslint errors on these.
- Do not run any git command. Do not run npm install.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
    ./node_modules/.bin/tsc -b
    ./node_modules/.bin/vitest run       (36 tests pass)
    ./node_modules/.bin/eslint src/pages/ModelViewPage.tsx   (0 errors)
