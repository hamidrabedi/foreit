TASK 5.7: remove the advertised-but-dead notifications UI (frontend).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY:
  src/hooks/useNotifications.ts          (DELETE)
  src/components/layout/TopBar.tsx       (remove the notifications button)
  src/pages/FormPlaygroundPage.tsx       (ONLY if the "Enable notifications" demo text implies a real feature — see below)

=== FACTS (verified) ===
- src/hooks/useNotifications.ts opens an EventSource to `${protocol}//${host}/api/admin/events` and
  console.logs every event. It has ZERO callers (grep useNotifications -> only its own file).
- The Go side has core.NotificationHub with an SSEHandler, but it is never mounted on a route and nothing
  calls Notify. So no notifications can ever arrive.
- src/components/layout/TopBar.tsx (~line 75-90) renders a bell-style button with
  title="Notifications" aria-label="Notifications (none)". It advertises a feature that does not exist.
The owner's decision: notifications are planned for later; do not ship dead UI that implies they work.

=== FIX ===
1. Delete src/hooks/useNotifications.ts.
2. TopBar.tsx: remove the notifications button element and any now-unused imports (icon, Tooltip wrapper
   if only used by it). Keep every other control, its order, and spacing intact.
3. FormPlaygroundPage.tsx: it is a form-controls playground; a switch labelled "Enable notifications" is
   just sample form content — LEAVE IT unless it imports or calls notification code. Report which.
4. grep -rn "useNotifications\|/api/admin/events" src  must print nothing.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run
  ./node_modules/.bin/eslint src/components/layout/TopBar.tsx   (0 errors)
If tsc errors appear ONLY in files another agent may be editing (src/lib/admin-prefix.ts, src/main.tsx,
src/api/client.ts, vite.config.ts, src/components/list/*), ignore them.

=== HARD CONSTRAINTS ===
- NO git commands. No npm install.
- Final report: what was removed (with line ranges), gate output.
