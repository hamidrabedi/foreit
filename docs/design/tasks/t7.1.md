TASK 7.1: Playwright design/health sweep with a MOCKED admin API (no Go backend required).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may CREATE: e2e/design-sweep.spec.ts, e2e/fixtures/admin-api-mock.ts
File you may MODIFY: playwright.config.ts (only: add a `mobile` project)

Playwright 1.63 and chromium are installed (~/.cache/ms-playwright). The existing
e2e/admin-crud-flow.spec.ts needs a real backend; this sweep must NOT — it mocks every
`/admin/api/**` request with page.route so it runs on any machine.

=== STEP 1: playwright.config.ts ===
Keep everything. Add a second project:
    { name: 'mobile', use: { ...devices['Desktop Chrome'], viewport: { width: 375, height: 812 } } },

=== STEP 2: e2e/fixtures/admin-api-mock.ts ===
Export `async function mockAdminApi(page: Page)` that:
  - sets a fake token before any script runs:
      await page.addInitScript(() => localStorage.setItem('admin_token', 'e2e-token'));
  - routes requests. Read src/api/client.ts for the exact paths the UI calls (base "/admin/api") and
    src/api/types.ts for response shapes. Respond with JSON for at least:
      GET  **/admin/api/config            -> { plugins: [], user: { name: "E2E Admin", role: "Super Admin" } }
      GET  **/admin/api/meta              -> { models: [ categories, products ], plugins: [] }  (ModelListMetadata: name,
                                             verbose_name, verbose_name_plural, count, permissions{add,change,delete,view:true})
      GET  **/admin/api/meta/categories   -> full Metadata: fields name(string, required), slug(string),
                                             is_active(boolean), created_at(datetime, read_only); relations [];
                                             permissions all true; actions []; filters [{name:"is_active",type:"boolean",label:"Active"}];
                                             list_display ["id","name","slug","is_active","created_at"];
                                             pagination {page_size:20,max_page_size:100}
      GET  **/admin/api/categories?**     -> PaginatedResponse with 3 rows (one with slug null to exercise EmptyValue)
      GET  **/admin/api/saved-views/**    -> { views: [] }
      GET  **/admin/api/search?**         -> { results: [] }
      anything else under **/admin/api/** -> 200 {}
  Use `route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify(...) })`.
  Register the catch-all FIRST and specific routes AFTER (Playwright matches the most recently added route first).

=== STEP 3: e2e/design-sweep.spec.ts ===
Determine the app's URL paths from the existing admin-crud-flow.spec.ts (it uses '/', '/login',
`/${MODEL}`) — use the same style.

ROUTES = dashboard '/', registry '/registry', list '/categories', create '/categories/create'.
For theme in ['light','dark'] × each route, one test:
  - collect console errors (`page.on('console')` type 'error') and `page.on('pageerror')`;
    IGNORE messages containing "Download the React DevTools" or "favicon".
  - await mockAdminApi(page); await page.goto(route);
  - apply theme: await page.evaluate(t => document.documentElement.classList.toggle('dark', t === 'dark'), theme)
  - await expect(page.locator('main')).toBeVisible();
    and await expect(page.locator('h1').first()).toBeVisible();
  - no horizontal overflow:
      const overflow = await page.evaluate(() => document.documentElement.scrollWidth - document.documentElement.clientWidth);
      expect(overflow).toBeLessThanOrEqual(1);
  - await page.screenshot({ path: testInfo.outputPath(`${route.name}-${theme}.png`), fullPage: true });
  - expect(errors).toEqual([]);
The `mobile` project automatically re-runs every test at 375px.

=== RUN IT ===
  ./node_modules/.bin/playwright test e2e/design-sweep.spec.ts --reporter=list
(the config's webServer starts `npm run dev` on :3000 and reuses an existing one.)
Report pass/fail per test. If a test FAILS because the UI genuinely overflows horizontally or logs a
console error, do NOT weaken the assertion and do NOT edit application code — report the exact route,
theme, viewport, and error/overflow value. Test-harness mistakes (wrong mock path/shape) you should fix.

=== VERIFY ===
  ./node_modules/.bin/tsc -b   (if e2e is not in the tsconfig project, run ./node_modules/.bin/tsc --noEmit -p . or skip and say so)
  ./node_modules/.bin/eslint e2e/design-sweep.spec.ts e2e/fixtures/admin-api-mock.ts   (0 errors)

=== HARD CONSTRAINTS ===
- NO git commands. No npm install. Do not edit src/.
- Kill the dev server you started when done if Playwright did not stop it.
- Final report: per-test results table (route × theme × project), any genuine UI defects found.
