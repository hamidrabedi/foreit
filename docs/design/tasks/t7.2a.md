TASK: Remove dead code and clear design-token lint violations (batch A).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web

STEP 1 — DELETE src/components/widgets/StatsCard.tsx.
It was superseded by src/components/widgets/stat-tile.tsx. Verify first with
`grep -rn "StatsCard" src/` — the ONLY hit should be the file itself. If anything
else imports it, STOP and report instead of deleting.

STEP 2 — Fix all `no-restricted-syntax` violations in these files ONLY:
  src/features/dashboard/widgets/ActivityWidget.tsx     (3)
  src/hooks/useKeyboardShortcuts.tsx                    (2)
  src/pages/ModelRegistryPage.tsx                       (2)
  src/components/DynamicModelPage.tsx                   (1)
  src/components/form/InlineRelations.tsx               (1)
  src/components/layout/ThemeToggle.tsx                 (1)
  src/features/dashboard/widgets/StatsWidget.tsx        (1)
  src/features/theme/ThemeCustomizer.tsx                (1)
  src/pages/FormPlaygroundPage.tsx                      (1)
  src/pages/ModelUpsertPage.tsx                         (1)

Find them with: npx eslint <file> and read the error messages — each one names the
offending literal.

=== TOKEN MAPPING (use these, do not invent) ===
Palette color -> semantic token:
  emerald-* / green-*            -> success        (bg: bg-success-surface, border: border-success/20, text: text-success)
  rose-* / red-*                 -> danger         (bg-danger-surface, border-danger/20, text-danger)
  amber-* / yellow-* / orange-*  -> warning        (bg-warning-surface, border-warning/20, text-warning)
  blue-* / sky-* / cyan-*        -> info           (bg-info-surface, border-info/20, text-info)
  indigo-* / violet-* / purple-* -> primary        (bg-primary/10, border-primary/20, text-primary)
  zinc-* / slate-* / gray-* / neutral-* / stone-*:
      as body text      -> text-foreground
      as secondary text -> text-muted-foreground
      as a surface      -> bg-surface-sunken  (or bg-surface-2 for a card)
      as a border       -> border-border-subtle  (or border-border)
Raw hex in a chart -> hsl(var(--chart-N)) via src/lib/chart-theme.tsx (import chartColor / CHART_COLORS).
Raw hex anywhere else -> the nearest semantic token above.
Arbitrary font size -> the type scale:
  text-[10px]/[11px] -> text-micro      text-[12px] -> text-meta     text-[13px] -> text-body
  text-[14px]        -> text-ui         text-[16px] -> text-lead     larger -> text-title / text-display
NOTE: arbitrary SPACING like translate-y-[2px] or min-w-[8rem] is FINE and is not flagged.

=== HARD CONSTRAINTS ===
- Change ONLY colors and font-size classes. Do NOT restructure, rename, or change behaviour.
- Do NOT remove or rename any data-testid, id, or aria-* attribute.
- Do NOT weaken or disable the eslint rule. Fix the code, not the rule.
- Only if a violation is genuinely unavoidable, add a targeted
  `// eslint-disable-next-line no-restricted-syntax` WITH a one-line reason.
- Do not run any git command. Do not run npm install.

=== VERIFY ===
    npx eslint <the files you changed>   -> 0 errors
    npx tsc -b                            -> no errors
    npx vitest run                        -> all 36 tests pass

=== EXACT VIOLATIONS (precomputed — do NOT run eslint to find them) ===
rtk intercepts `npx eslint` and writes the full log outside the repo where you
cannot read it. That is why this list is given to you. To verify afterwards, use
`./node_modules/.bin/eslint <file>` (bypasses the wrapper), NOT `npx eslint`.

src/features/dashboard/widgets/ActivityWidget.tsx:
  line 15 (palette color): { id: '4', user: 'Alex Johnson', action: 'Deleted order #4521', time: '3 hours ago' },
  line 26 (font size): <div className="w-8 h-8 rounded-full bg-accent flex items-center justify-center text-[10px] font-bold">
  line 37 (font size): <p className="text-[10px] text-muted-foreground">{item.time}</p>

src/hooks/useKeyboardShortcuts.tsx:
  line 215 (font size): <kbd className="px-2 py-1 bg-muted/80 border border-border/60 rounded text-[11px] font-mono font-semibold text
  line 222 (font size): <div className="text-[11px] text-muted-foreground text-center pt-2 border-t border-border/40">

src/pages/ModelRegistryPage.tsx:
  line 122 (font size): <span className="font-semibold text-[14px] truncate">
  line 125 (font size): <span className="shrink-0 text-[11px] tabular-nums text-muted-foreground">

src/components/DynamicModelPage.tsx:
  line 25 (palette color): <div className="p-8 text-center text-red-500">

src/components/form/InlineRelations.tsx:
  line 171 (font size): <p className="text-[11px] text-muted-foreground px-1 leading-relaxed">

src/components/layout/ThemeToggle.tsx:
  line 37 (palette color): <Sun className="h-4 w-4 transition-all text-yellow-400" />

src/features/dashboard/widgets/StatsWidget.tsx:
  line 23 (palette color): isNegative ? "bg-destructive/10 text-destructive" : "bg-green-500/10 text-green-600 dark:text-green-400"

src/features/theme/ThemeCustomizer.tsx:
  line 141 (font size): <p className="mt-2 text-[11px] text-muted-foreground">

src/pages/FormPlaygroundPage.tsx:
  line 14 (palette color): const [color, setColor] = useState("#6366f1");

src/pages/ModelUpsertPage.tsx:
  line 443 (font size): <p className="text-[11px] text-muted-foreground px-1 leading-relaxed">

TOTAL in this batch: 14
