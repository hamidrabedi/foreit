TASK: Fix the bundle-budget breach (task 7.3).

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify: vite.config.ts  (and ONLY if needed, the route files named below)

=== THE PROBLEM ===
A production build measures roughly:
    JS  1,813 kB raw / 501 kB gzipped     <-- the cap is 300 kB gzipped
    CSS   105 kB raw /  27.8 kB gzipped   <-- fine
The project's own rule (rules/web/performance.md) caps an app page at 300 kB gzipped JS.
Everything currently lands in ONE chunk.

=== FIX 1: manual chunking (do this first, measure after) ===
In vite.config.ts add a `build.rollupOptions.output.manualChunks` mapping that splits
the big, rarely-changing vendors into their own chunks:

    manualChunks: {
      react: ["react", "react-dom"],
      router: ["@tanstack/react-router"],
      query: ["@tanstack/react-query"],
      charts: ["recharts"],
      motion: ["framer-motion"],
      radix: [
        "@radix-ui/react-dialog",
        "@radix-ui/react-dropdown-menu",
        "@radix-ui/react-select",
        "@radix-ui/react-tabs",
        "@radix-ui/react-tooltip",
        "@radix-ui/react-alert-dialog",
        "@radix-ui/react-label",
        "@radix-ui/react-slot",
      ],
      forms: ["react-hook-form", "@hookform/resolvers", "zod"],
    }

This project uses rolldown (see the `overrides` in package.json), so if
`build.rollupOptions` is not honoured, use `build.rolldownOptions` with the same
shape. Try rollupOptions first; if the build warns that it is ignored, switch.

Also raise the warning threshold to something honest rather than silencing it:
    build.chunkSizeWarningLimit: 600

=== FIX 2: lazy-load recharts (the single biggest win) ===
`recharts` is large and is only needed by chart widgets. Convert the chart components
to be loaded lazily so they are NOT in the initial bundle:

- In src/components/widgets/chart-widget.tsx and
  src/components/sdui/widgets/ChartWidget.tsx, keep the implementations as they are.
- Create thin lazy wrappers where they are CONSUMED, using React.lazy + Suspense:
      const ChartWidget = React.lazy(() => import("./ChartWidget"))
  and wrap the usage in
      <Suspense fallback={<Skeleton className="h-[280px] w-full" />}>
  using the existing Skeleton component (src/components/ui/skeleton.tsx).
- Do the same for the sparkline inside src/components/widgets/stat-tile.tsx ONLY if
  it is straightforward; if it complicates the component, leave stat-tile alone and
  say so.

Find the consumers with a grep for ChartWidget / chart-widget before editing.

=== MEASURE AND REPORT ===
Run `./node_modules/.bin/vite build` BEFORE and AFTER and report the gzipped size of
each emitted chunk both times. State plainly whether the largest initial-load chunk is
now under 300 kB gzipped. If it is NOT, say so — do not claim success you did not measure.

=== HARD CONSTRAINTS ===
- Do NOT change any component's behaviour or visual output.
- Do NOT remove any dependency.
- Do not run any git command. Do not run npm install.
- No Tailwind palette colors, no raw hex, no arbitrary font sizes (eslint errors).

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
    ./node_modules/.bin/tsc -b
    ./node_modules/.bin/vitest run      (36 tests pass)
    ./node_modules/.bin/vite build      (succeeds)
