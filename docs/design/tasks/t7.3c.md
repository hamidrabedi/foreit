TASK 7.3c: stop bundling the entire lucide-react icon set into the entry chunk.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY:
  src/features/dashboard/widgets/StatsWidget.tsx

=== THE PROBLEM ===
src/features/dashboard/widgets/StatsWidget.tsx line 2:

    import * as LucideIcons from 'lucide-react';
    const IconComponent = icon ? (LucideIcons as any)[icon] : null;
    ... <LucideIcons.ArrowDownRight .../> ... <LucideIcons.ArrowUpRight .../>

A namespace import with a dynamic string lookup defeats tree-shaking, so every lucide icon
(~100 kB gzipped) lands in the entry chunk. StatsWidget is registered eagerly in src/lib/boot.ts,
so this hits every page's first load. Measured initial JS: 430 kB gzipped vs a 300 kB budget.

The project already has a curated, tree-shakeable icon resolver:
src/components/ModelIcon.tsx exports `ModelIcon` ({ name, className }) with a fixed ICONS map and
a HelpCircle fallback. Read it first.

=== FIX ===
1. Replace the namespace import with named imports: `import { ArrowDownRight, ArrowUpRight } from 'lucide-react';`
   and use `<ArrowDownRight .../>` / `<ArrowUpRight .../>` with the SAME classNames.
2. Replace the dynamic lookup: wherever IconComponent is rendered, render
   `<ModelIcon name={icon} className={<the same className IconComponent had>} />`
   only when `icon` is truthy (preserve the existing conditional). Import ModelIcon from
   '../../../components/ModelIcon' (verify the relative path).
3. Remove the `as any` cast. No other changes.

=== MEASURE ===
Run `./node_modules/.bin/vite build` and report the gzipped size of the entry `index-*.js` chunk
and the sum of all eagerly loaded chunks, before-vs-after is not required — just report after,
and state plainly whether initial load is now under 300 kB gzipped. Do not claim what you did
not measure.

=== VERIFY (use ./node_modules/.bin/, NOT npx) ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run
  ./node_modules/.bin/eslint src/features/dashboard/widgets/StatsWidget.tsx   (0 errors)
  grep -rn "import \* as .* from 'lucide-react'" src   (must print nothing)
If tsc reports errors only in src/pages/ModelUpsertPage.tsx or src/api/*, ignore them — another
agent is editing those.

=== HARD CONSTRAINTS ===
- NO git commands. No npm install.
- No palette colors, raw hex, or text-[Npx].
- Final report: diff summary, build sizes, gate output.
