TASK 7.3d: the lazy recharts chunk is still preloaded on first page load — fix it and measure.

Working directory: /home/hamid/Other/projects/foreit/forge/admin/ui/web
Files you may modify — ONLY:
  vite.config.ts
  (and, ONLY if the diagnosis proves it necessary, the single file that statically imports recharts
   into the eager graph — name it in your report)

=== MEASURED FACTS (2026-09-13, after tasks 7.3 / 7.3b / 7.3c) ===
`vite build` emits ../dist/index.html whose modulepreload list is:
  index 62.08 kB gz, rolldown-runtime 0.42, react 59.61, router 26.14, charts 112.99, card 8.93,
  radix 37.94, query 9.61, motion 39.41      => eager total ≈ 357 kB gz (budget 300 kB)
`charts-*.js` (recharts) should NOT be eager: every module importing recharts is lazy-loaded
(src/features/dashboard/widgets/ChartWidget.tsx, src/components/sdui/widgets/ChartWidget.tsx,
src/components/widgets/chart-widget.tsx, src/components/widgets/stat-sparkline.tsx — all behind
React.lazy). Without charts the eager total would be ≈ 244 kB gz — under budget.

vite.config.ts uses rolldown `build.rolldownOptions.output.codeSplitting.groups` including
  { name: "charts", test: /node_modules[\\/]recharts[\\/]/ }
Hypothesis: the manual group pulls modules shared with the eager graph (or the group itself) into a
chunk the entry depends on, so index.html preloads it.

=== STEPS ===
1. Build once and record the modulepreload list: 
     ./node_modules/.bin/vite build
     grep -o 'assets/[^"]*\.js' ../dist/index.html
2. Find what pulls charts into the entry: inspect the emitted entry chunk
   (grep -o 'charts-[A-Za-z0-9_-]*\.js' ../dist/assets/index-*.js) and check whether the entry
   imports it statically (an `import ... from "./charts-..."` at top level) vs only via a dynamic
   `import()`.
3. Try, in order, rebuilding and re-checking after each; stop at the first that removes charts from
   index.html's modulepreload list:
   a. Remove the `charts` group from codeSplitting.groups (let recharts fall into the lazy widget chunks).
   b. If (a) moves recharts into the entry instead, restore the group and add to it whatever option
      rolldown offers to NOT pull shared dependencies into the group (check rolldown docs in
      node_modules/rolldown for `codeSplitting` group options such as a priority/minShareCount/
      includeDependenciesRecursively-style flag) — use only options that actually exist in the
      installed rolldown version's type definitions.
   c. If a specific eager file statically imports something from a recharts-dependent module, make
      that import lazy/type-only (this is the one extra file you may touch).
4. Keep every other group (react, router, query, motion, radix, forms) unchanged.

=== MEASURE AND REPORT (mandatory) ===
Report: the final modulepreload list from ../dist/index.html, the gzipped size of each of those
chunks from the build output, and the arithmetic sum. State plainly whether the eager total is under
300 kB gzipped. Also confirm the charts chunk still exists as a separate lazy chunk.
Do not claim anything you did not measure.

=== VERIFY ===
  ./node_modules/.bin/tsc -b
  ./node_modules/.bin/vitest run
If vite build or tsc fails ONLY because of src/pages/ModelListPage.tsx or src/components/list/*
(another agent is editing those), wait ~30 seconds and rebuild; do not edit those files.

=== HARD CONSTRAINTS ===
- NO git commands. No npm install. Do not remove dependencies.
- Final report: what caused it, what you changed, measurements.
