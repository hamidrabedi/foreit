TASK 7.4: update the ecommerce Playwright suite for the redesigned Radix Select controls (test code only).

Working directory: /home/hamid/Other/projects/foreit/examples/ecommerce/ui-tests
File you may modify — ONLY: tests/admin-redesign.spec.ts

=== CONTEXT ===
The admin redesign replaced native <select> elements with Radix Select (a <button role="combobox"> trigger;
options render in a portal only while open, as elements with role="option").
Running `playwright test` against the live ecommerce server gives 14 passed / 2 failed, both with
    page.selectOption: Error: Element is not a <select> element
  1) admin-redesign.spec.ts:118   await page.selectOption('[data-testid="page-size-select"]', '10');
  2) admin-redesign.spec.ts:135   await page.selectOption('[data-testid="filter-is_active"]', 'true');
The app behaviour is correct; only the test interaction is outdated.

Facts from the UI source (forge/admin/ui/web, read-only for you):
- page-size trigger: data-testid="page-size-select", options are the numbers (e.g. "10", "20", "50", "100").
- boolean filter trigger: data-testid="filter-is_active"; items are "All" (value __all__) plus the true/false
  items — read /home/hamid/Other/projects/foreit/forge/admin/ui/web/src/components/list/ListFilterPanel.tsx
  around line 118-140 to get their exact visible labels.
- Saving a view auto-selects it, so the trigger `saved-view-select` shows the view name; those assertions are fine.

=== FIX ===
Add a small helper at the top of the spec (after imports):

    async function chooseRadixOption(page, triggerTestId: string, optionName: string | RegExp) {
      await page.getByTestId(triggerTestId).click();
      await page.getByRole('option', { name: optionName, exact: typeof optionName === 'string' }).click();
    }

Replace ONLY the two selectOption calls:
  1) await chooseRadixOption(page, 'page-size-select', '10');
  2) await chooseRadixOption(page, 'filter-is_active', <exact visible label of the "true" item>);
Keep the surrounding waitForResponse / expect lines unchanged. Do not change any other test.

=== VERIFY ===
The suite needs the live server (its playwright.config webServer runs `go run -tags embed .` on :8020, and
forge/admin/ui/dist is already built). Run only the two affected tests:
  ./node_modules/.bin/playwright test tests/admin-redesign.spec.ts -g "list:" --reporter=line
Both must pass. If one fails for a reason other than the Select interaction, report the exact error and
screenshot path instead of weakening assertions.

=== HARD CONSTRAINTS ===
- NO git commands. No npm install. Do not edit application code.
- Final report: the diff and the playwright output.
