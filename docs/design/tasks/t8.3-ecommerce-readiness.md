TASK 8.3: make the ecommerce example "ready to go" with the redesigned admin UI (Makefile + docs only).

Working directory: /home/hamid/Other/projects/foreit/examples/ecommerce
Files you may modify — ONLY:
  Makefile
  README.md
  SETUP.md

=== FACTS (verified; do not change application code) ===
- The admin UI source is /home/hamid/Other/projects/foreit/forge/admin/ui/web (Vite + React). `npm run build`
  there outputs to /home/hamid/Other/projects/foreit/forge/admin/ui/dist (vite.config.ts outDir "../dist").
  `dist/` is NOT committed (gitignored), so a fresh clone has no built UI.
- examples/ecommerce/main.go (~line 172-196) serves the admin UI from a static directory if one exists, trying
  `admin.static_dir` from config, then ../../forge/admin/ui/dist, ../forge/admin/ui/dist, ./dist, static/admin.
  So after building the UI, a plain `go run .` serves the real admin — no build tag needed.
- Building with `-tags embed` embeds forge/admin/ui/dist into the binary (forge/admin/ui/ui.go). Without the tag and
  without a built dist, only a placeholder stub page (forge/admin/ui/stub/index.html) is served.
- Config defaults (config/config.yaml): SQLite database ./ecommerce.sqlite, admin path /admin/, admin user
  admin / admin123. The UI test suite expects the server on http://localhost:8020 (ui-tests/playwright.config.ts
  starts `go run -tags embed .` itself and reuses a running server). Check which config key / env var sets port
  8020 (grep main.go and config/config.yaml for 8020 / FORGE_PORT / server.port) and document it accurately.
- Existing Makefile `run` uses `forge runserver` (requires the Forge CLI); `build` runs
  `go build -o ecommerce -ldflags="-s -w" .`.
- ui-tests/ has package.json with @playwright/test and uses the system Chrome channel.

=== CHANGES ===
1. Makefile — add targets (keep all existing targets; add them to the `help` output in the file's existing style):
     ADMIN_UI_DIR := ../../forge/admin/ui/web
     admin-ui:        ## Build the admin UI bundle (forge/admin/ui/dist)
     	cd $(ADMIN_UI_DIR) && npm ci && npm run build
     run-go:          ## Run the server with plain `go run` (serves the built admin UI from ../../forge/admin/ui/dist)
     	go run .
     build-embed: admin-ui   ## Production binary with the admin UI embedded
     	go build -tags embed -o ecommerce -ldflags="-s -w" .
     ui-test:         ## Run the Playwright admin suite (needs Chrome; starts the server itself)
     	cd ui-tests && npm ci && npx playwright test
     demo: admin-ui run-go   ## One command: build the admin UI and start the server
   Use real TAB indentation for recipe lines. Mark new targets .PHONY in the file's existing style.
   Verify with `make -n admin-ui run-go build-embed ui-test demo` (dry run prints the commands; must not error).

2. README.md — add a section "## Admin UI" right after the quick-start/run section (read the file to find the
   right place; do not duplicate existing headings). Contents:
   - one paragraph: the admin is a React app served by the Go server at http://localhost:8020/admin/
     (use the real port you verified), login admin / admin123 from config/config.yaml;
   - "Quick start" code block:  make admin-ui  →  make run-go   (or: make demo)
   - "Production binary" code block: make build-embed  →  ./ecommerce
   - a note that without building the UI you will only see a placeholder page;
   - "End-to-end tests" code block: make ui-test (Chrome required; the suite starts the server);
   - "Mounting under a different path": set `admin.path` in config/config.yaml; the server injects the prefix into the
     UI at runtime (no rebuild needed).
3. SETUP.md — in the step list, add a step "Build the admin UI" with `make admin-ui` before the run step, and change the
   run instruction to mention `make run-go` (keep the `forge runserver` option as an alternative if already documented).

=== VERIFY ===
  make -n admin-ui run-go build-embed ui-test demo
  make help   (new targets listed, no error)
  grep -n "make admin-ui" README.md SETUP.md

=== HARD CONSTRAINTS ===
- NO git commands. Do not edit any .go, .ts, config, or package files.
- Keep existing content; add, don't rewrite. Match the files' existing tone and formatting.
- Final report: diff summary and the dry-run output.
