package promotions

import "context"

// RegisterAdmin registers models with the admin interface.
// Stubbed for now to keep example compilation stable.
func RegisterAdmin(ctx context.Context) {
	_ = ctx
}
