package {{.AppName}}

import (
	"github.com/forgego/forge/pkg/schema"
)

// {{.ModelName}} represents a {{.ModelName}} model
type {{.ModelName}} struct {
	schema.BaseSchema
}

// Fields returns all field definitions for {{.ModelName}}
func ({{.ModelName}}) Fields() []schema.Field {
	return []schema.Field{
		schema.Int64("id").WithPrimary().WithAutoIncrement(),
		// Add your fields here
	}
}

// Meta returns model metadata
func ({{.ModelName}}) Meta() schema.Meta {
	return schema.Meta{
		TableName:        "{{.TableName}}",
		VerboseName:      "{{.ModelName}}",
		VerboseNamePlural: "{{.ModelNamePlural}}",
	}
}

// Relations returns all relationship definitions
func ({{.ModelName}}) Relations() []schema.Relation {
	return []schema.Relation{}
}

// Hooks returns model lifecycle hooks
func ({{.ModelName}}) Hooks() *schema.ModelHooks {
	return nil
}



