Implemented one focused high-impact batch on admin REST reliability: bulk update/delete TODOs are now fully implemented and tested.

### Changed
- `forge/admin/api/rest/router.go`
  - Implemented `handleBulkUpdate`:
    - validates `{ ids, data }`
    - normalizes IDs (new `normalizeBulkID`)
    - fetches object + checks object-level change permission
    - best-effort updates with per-item error collection
    - returns `200` (full), `207` (partial), `400`/`500` terminal failures
  - Implemented `handleBulkDelete`:
    - validates `{ ids }`
    - normalizes IDs
    - fetches object + checks object-level delete permission
    - best-effort deletes with per-item error collection
    - returns `204` (full), `207` (partial), `400`/`500` terminal failures
- `forge/admin/api/rest/router_test.go`
  - Added targeted tests for bulk update/delete:
    - success paths
    - partial-failure multi-status paths
    - invalid payload rejection
    - full-failure terminal error paths
- `ops/codex-24x7/STATUS.md`
  - Updated `Completed this run`, `Remaining work`, `Next run plan`.
- `ops/codex-24x7/HISTORY.md`
  - Appended concise run log for this batch.

### Tests run
- `go test ./admin/api/rest` in `forge` (pass)
- `go test ./...` in `examples/ecommerce` (pass; no test files)

### Note
- Initial `go test` attempts failed due Go build cache access at `C:\Users\hamid\AppData\Local\go-build`; reran successfully with `GOCACHE=C:\Users\hamid\AppData\Local\Temp\go-build`.
- I also observed many unrelated pre-existing worktree changes and did not modify/revert them.