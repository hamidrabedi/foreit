Implemented one focused high-impact batch on framework auth scaffolding TODO debt.

**What changed**
- Closed auth scaffold TODOs in `forge/cli/commands/project/auth.go`:
  - Added generated login/logout endpoint registration in scaffolded `api.go`:
    - `POST /api/v1/auth/login`
    - `POST /api/v1/auth/logout`
  - Added generated handlers `handleLogin`/`handleLogout` with request validation and JSON responses.
  - Added generated JWT helper `generateJWTToken(...)` (HMAC-SHA256 scaffold) and `writeJSON(...)`.
- Added tests in `forge/cli/commands/project/auth_test.go`:
  - Validates scaffold output includes login/logout routes and JWT helper.
  - Validates TODO placeholders are removed from generated auth API file.
  - Validates existing-app guard behavior (`auth app already exists`).

**Run tracking updated**
- Updated `ops/codex-24x7/STATUS.md` (Completed this run / Remaining work / Next run plan).
- Appended run log to `ops/codex-24x7/HISTORY.md`.

**Tests run**
- `go test ./cli/commands/project -count=1` in `forge` (pass)
- `go test ./... -count=1` in `examples/ecommerce` (pass)